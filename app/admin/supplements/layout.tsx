export const metadata = {
  title: "Supplements",
  description: "Manage the StackItUp supplement library — create, edit, and delete supplements.",
};

export default function SupplementsAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
