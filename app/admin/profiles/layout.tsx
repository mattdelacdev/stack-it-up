export const metadata = {
  title: "Profiles",
  description: "View user profiles and change roles across the StackItUp community.",
};

export default function ProfilesAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
