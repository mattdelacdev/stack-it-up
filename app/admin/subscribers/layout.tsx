export const metadata = {
  title: "Subscribers",
  description: "Manage newsletter subscribers — view signups, edit details, and remove entries.",
};

export default function SubscribersAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
